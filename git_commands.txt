git init
git add
git status
git commit -m "message"
git restore filename
git rm --cached filename


push to git hub 
1- we need repo on github 
for existing repo 
1- git remote add origin <url of the repo>
2- git branch -M main
3- git push -u origin main

udpated from the GITHUB

branch
git branch dev - new branch
git switch dev
